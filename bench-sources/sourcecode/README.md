# Sourcecode

Copied from https://github.com/com-lihaoyi/sourcecode/tree/0.4.4.

Tests can be run via `sbt "benchSourcecode/runMain sourcecode.Main"`. The existing [test/src/sourcecode/Main.scala](test/src/sourcecode/Main.scala) entry point is used to run the tests without a separate test configuration.
