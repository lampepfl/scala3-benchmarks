name: Benchmark PR

on:
  workflow_dispatch:
    inputs:
      pr:
        description: "Dotty PR number"
        required: true
        type: string
      jvm:
        description: "JVM to use"
        required: false
        default: "temurin:25"
        type: string
      runs:
        description: "Number of runs per version"
        required: false
        default: "1"
        type: string
      filter:
        description: "JMH benchmark filter (regex pattern)"
        required: false
        default: ""
        type: string
  workflow_call:
    inputs:
      pr:
        description: "Dotty PR number"
        required: true
        type: string
      jvm:
        description: "JVM to use"
        required: false
        default: "temurin:25"
        type: string
      runs:
        description: "Number of runs per version"
        required: false
        default: "1"
        type: string
      filter:
        description: "JMH benchmark filter (regex pattern)"
        required: false
        default: ""
        type: string

jobs:
  publish-pr:
    runs-on: [self-hosted, benchmarks]
    outputs:
      version: ${{ steps.version.outputs.version }}
    steps:
      - name: Checkout dotty PR
        uses: actions/checkout@v4
        with:
          repository: scala/scala3
          ref: refs/pull/${{ inputs.pr }}/merge
          path: dotty

      - name: Setup Coursier
        uses: coursier/setup-action@v1

      - name: Setup sbt
        uses: sbt/setup-sbt@v1

      - name: Publish dotty locally
        working-directory: dotty
        env:
          NIGHTLYBUILD: "yes"
        run: sbt community-build/prepareCommunityBuild

      - name: Get published version
        id: version
        working-directory: dotty
        run: |
          VERSION=$(cat community-build/scala3-bootstrapped.version)
          echo "Published version: $VERSION"
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"

  benchmark:
    needs: publish-pr
    uses: ./.github/workflows/benchmark.yml
    with:
      versions: ${{ needs.publish-pr.outputs.version }}
      jvm: ${{ inputs.jvm }}
      runs: ${{ inputs.runs }}
      filter: ${{ inputs.filter }}
      branch: pr${{ inputs.pr }}
    secrets: inherit
